# Place focal-paper PDFs here. Files in this directory are gitignored — each contributor obtains their own copies.

The extraction/embedding pipeline (`scripts/extract_and_embed.py`) reads from this directory.

